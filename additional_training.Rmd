# Additional Training

```{r additional_training.csv}
vroom::vroom('data/additional_training.csv') %>% 
  dplyr::filter(display == T) %>% 
  vitae::detailed_entries(what = with, when, with = what, where, why)
```